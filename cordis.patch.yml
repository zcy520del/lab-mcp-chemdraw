# NOTE: set `command` to your real python and replace args[0] with the
# installed absolute path of server/server.py (pnpm link dirs vary).
- insert:
    - id: mcp-chemdraw
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        serverName: chemdraw
        transport: stdio
        command: python
        args: ['@zcy520del/lab-mcp-chemdraw/server/server.py']
